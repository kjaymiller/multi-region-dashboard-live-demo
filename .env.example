# Backend Database (for storing connection metadata and test history)
# Required PostgreSQL extensions: timescaledb, pg_stat_statements
# Use Docker Compose: docker compose --profile postgres up -d
# Or provide your own PostgreSQL database with extensions enabled
DATABASE_URL=postgresql://dashboard_user:dashboard_password@localhost:5432/dashboard

# Encryption key for database connection passwords
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
DB_PASSWORD_ENCRYPTION_KEY=your-generated-key-here

# Target Databases (databases being monitored by the dashboard)
# Recommended extension: pg_stat_statements (for comprehensive health checks)
# Without pg_stat_statements: Health checks will still work but with limited query statistics
# Aiven PostgreSQL Connection Strings (use PgBouncer connection strings on port 6543)
AIVEN_PG_US_EAST=postgresql://user:password@host:6543/defaultdb?sslmode=require
AIVEN_PG_EU_WEST=postgresql://user:password@host:6543/defaultdb?sslmode=require
AIVEN_PG_ASIA_PACIFIC=postgresql://user:password@host:6543/defaultdb?sslmode=require

# LaunchDarkly SDK Key
LAUNCHDARKLY_SDK_KEY=sdk-your-key-here

# TimescaleDB Retention Policies (for connection_tests hypertable)
# How long to keep uncompressed data before compression (default: 7 days)
TIMESCALE_COMPRESSION_AFTER_DAYS=7
# How long to keep data before automatic deletion (default: 90 days)
TIMESCALE_RETENTION_DAYS=90

# Ollama AI Service Configuration
# Set to false to disable chat functionality
CHAT_ENABLED=true
# Ollama service URL (update when using remote Ollama service)
OLLAMA_BASE_URL=http://localhost:11434
# Default AI model to use (e.g., llama3.2:latest, mistral:latest)
OLLAMA_MODEL=gpt-oss
